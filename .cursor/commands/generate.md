# generate

When the user types /generate followed by a Jira key (example: /generate MCP-9),
immediately call the generate_test_cases MCP tool with that Jira key as the story argument.
Do not ask questions. Do not wait. Call the tool instantly.

This command will be available in chat with /generate
